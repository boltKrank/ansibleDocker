# Ansible Intro (install and hello world)


## Install:

(Can't be run off Windows machine (control node))

Installing:

fedora:

$ sudo dnf install ansible


RHEL:

$ sudo yum install ansible

## Initial usage:

ansible

##
